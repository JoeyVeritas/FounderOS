import React from 'react'
import ReactDOM from 'react-dom/client'
import FounderOS from './FounderOS.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FounderOS />
  </React.StrictMode>
)
